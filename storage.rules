rules_version = '2';

// Firebase Storage Security Rules - No restrictions
service firebase.storage {
  match /b/{bucket}/o {
    // Allow all operations on all files
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}